langage: java
sudo: true
jdk: openjdk11

addon:
  sonnarcloud:
    token: $SECRETVALUE

script:
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent install sonar:sonar

after_deploy:
  - echo "done deploying"